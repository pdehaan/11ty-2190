<!doctype html>
<html lang="{{ locale }}">
  <head>
    <title>{{ title }}</title>
  </head>
  <body>
    {% set localeHeader = site[locale].header %}
    {% include localeHeader %}

    <main>
      {{ content | safe }}
    </main>

    {% include "components/footer.njk" %}
  </body>
</html>